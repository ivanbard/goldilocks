{
  "name": "goldilocks-kingston",
  "version": "1.0.0",
  "description": "Goldilocks Kingston — Arduino + Web App for finding the 'just right' balance of ventilation, cost savings, and environmental impact",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && node server.js",
    "dev:frontend": "cd frontend && npm run dev",
    "gateway": "cd gateway && node index.js",
    "seed": "cd backend && node db/seed.js",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../gateway && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
